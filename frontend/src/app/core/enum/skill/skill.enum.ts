import { environment } from "src/environments/environment";

export const SKILL_ROUTES = {
  createSkill: `${environment.api}/skill/createSkill`,
  deleteSkill: `${environment.api}/skill/deleteSkill/`,
}
